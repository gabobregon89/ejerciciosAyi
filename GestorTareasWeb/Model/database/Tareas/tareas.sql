CREATE TABLE ESTADOS 
(
  SID NUMBER(10) NOT NULL 
, ESTADOS VARCHAR2(255) 
, CONSTRAINT ESTADOS_PK PRIMARY KEY 
  (
    SID 
  )
  ENABLE 
);

CREATE TABLE TEMAS 
(
  SID NUMBER(10) NOT NULL 
, TEMA VARCHAR2(255) 
, DESCRIPCION VARCHAR2(255) 
, INCIDENCIAS VARCHAR2(255) 
, FK_ESTADO NUMBER(10) NOT NULL 
, CONSTRAINT TEMAS_PK PRIMARY KEY 
  (
    SID 
  )
  ENABLE 
);

CREATE TABLE NOTAS 
(
  SID NUMBER(10) NOT NULL 
, NOTA VARCHAR2(255) 
, FECHA DATE 
, FK_TEMA NUMBER(10) 
, CONSTRAINT NOTAS_PK PRIMARY KEY 
  (
    SID 
  )
  ENABLE 
);

ALTER TABLE NOTAS
ADD CONSTRAINT NOTAS_TEMAS_FK1 FOREIGN KEY
(
  FK_TEMA 
)
REFERENCES TEMAS
(
  SID 
)
ENABLE;

ALTER TABLE TEMAS
ADD CONSTRAINT TEMAS_ESTADOS_FK1 FOREIGN KEY
(
  FK_ESTADO 
)
REFERENCES ESTADOS
(
  SID 
)
ENABLE;
